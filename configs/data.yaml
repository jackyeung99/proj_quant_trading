storage:
  backend: local
  base_dir: "."

ingestion:
  provider: yfinance
  symbols: ["SPY", "XLE"]
  start: "2025-01-01"
  end: "2026-01-01"
  interval: "1d"
  auto_adjust: true

paths:
  bronze_prefix: "data/bronze/yfinance"
  processed_prefix: "data/processed"
  processed_returns_key: "data/processed/returns_daily.parquet"
