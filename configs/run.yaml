logging:
  level: INFO
  log_file: "artifacts/logs/qbt.log"

storage:
  config: "steps/storage.yaml"

sources:
  config: "steps/sources.yaml"


ingestion:
  enabled: False
  config: "steps/ingestion.yaml"

silver:
  enabled: False

gold:
  enabled: True
  config: "steps/build_model_table.yaml"

signal:
  enabled: False
  config: "strategies/run_multi_state.yaml"

execution: 
  enabled: False
  config: "steps/execution.yaml"

evaluation:
  enabled: True
  config: "steps/evaluation.yaml" 