storage:
  config: "steps/storage.yaml"

sources:
  config: "steps/sources.yaml"


ingestion:
  enabled: True
  config: "steps/ingestion.yaml"

silver:
  enabled: True

gold:
  enabled: True
  config: "steps/build_model_table.yaml"

signal:
  enabled: True
  config: "strategies/run_state.yaml"

execution:
  enabled: True
  config: "steps/execution.yaml"
